"""wulfenite.scripts — one-off dataset preparation utilities.

These modules are entry points meant to be invoked via ``python -m
wulfenite.scripts.<name>``. No re-exports here to avoid preloading
them into ``sys.modules`` and triggering ``runpy`` warnings.
"""
